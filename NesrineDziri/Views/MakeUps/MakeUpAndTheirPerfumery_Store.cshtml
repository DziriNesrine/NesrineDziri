@model IEnumerable<NesrineDziri.Models.MakeUp>

@{
    ViewData["Title"] = "MakeUpAndTheirPerfumery_Store";
}

<h1>MakeUpAndTheirPerfumery_Store</h1>

<p>
    <a class="btn btn-info"  asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nom)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Marque)
            </th>
            <th>
                Perfumery_Store Id
            </th>
            <th>
                 Perfumery_Store Name
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nom)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Marque)
            </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Perfumery_Store.Id)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Perfumery_Store.Name)
                </td>

                <td>
                    <a class="btn btn-warning" asp-action="Edit" asp-route-id="@item.Id">Edit</a>
                    <a class="btn btn-dark" asp-action="Details" asp-route-id="@item.Id">Details</a>
                    <a class="btn btn-danger" asp-action="Delete" asp-route-id="@item.Id">Delete</a>
                </td>
        </tr>
}
    </tbody>
</table>
